import 'package:flutter/material.dart';

class PlaudeSpacing {
  const PlaudeSpacing._();

  static const xs = 4.0;
  static const sm = 8.0;
  static const md = 12.0;
  static const lg = 16.0;
  static const xl = 24.0;
  static const xxl = 32.0;
  static const xxxl = 48.0;

  static const screenGutter = EdgeInsets.all(20);
  static const screenGutterDense = EdgeInsets.all(16);
  static const cardInset = EdgeInsets.all(20);
  static const cardInsetCompact = EdgeInsets.all(16);
  static const chipPadding = EdgeInsets.symmetric(horizontal: 12, vertical: 8);
}
